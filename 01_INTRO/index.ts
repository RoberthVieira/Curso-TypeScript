const firstName = 'Roberth';
const anotherNome = 1;
const x = true;

function greeting(name: string) {
    console.log('Olá ' + name);
};

greeting(firstName);
//greeting(anotherNome);
//greeting(x);